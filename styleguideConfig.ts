import { Config } from './types'

// Сокращения:
//   * line-height: lhxs, lhs, lhm, lhl
// * font-size: fz2xs, fzxs, fzs, fzm, fzl, fzxl
// * color (typo-colors): colpr (typo-primary), colsec (typo-secondary), colsuc/colwar/colerr
// * background-color, fill, stroke (bg-colors): bgp (bg-primary), bgs (bg-secondary)
// * spacing (padding, margin, top, gap)
// * padding: pxs, pbxs, ptxs, plxs, prxs
// * margin: mxs, mbxs, mtxs, mlxs, mrxs
// * top и gap без шорткатов, но с запретов в стайллинте
// * border
// * border-radius

// todo use colord for color manipulations

export const styleguideConfig: Config = {
  name: 'Styleguide',
  properties: [],
}
